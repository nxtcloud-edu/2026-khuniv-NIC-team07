# RE:PLAN 현재 버전 기능 및 작동 방식 안내서

> - 문서 버전: `0.5.1`
> - 기준 브랜치 및 커밋: `main` / `06f3c12` (`feat: add freeform planning preferences`)
> - 포함 범위: 자연어 계획 요구사항 입력과 GPT 계획 생성 기준 설명까지 포함
> 대상: 개발 지식이 없는 사용자와 팀원  
> 서비스 기본 시간대: `Asia/Seoul`

## 1. RE:PLAN은 어떤 서비스인가?

RE:PLAN은 시험일까지 공부해야 할 범위를 OpenAI의 GPT가 날짜와 시간별 계획으로 만들어 주고, 사용자가 실제로 공부한 결과를 기록하면 남은 계획을 다시 만들어 주는 학습 캘린더다.

단순히 시험 범위를 남은 날짜 수로 나누는 프로그램이 아니다. 시험 정보, 목표 회독 수, 우선해서 공부할 챕터, 사용자가 자유롭게 적은 과목 특성·계획 요구사항, 수업·아르바이트 같은 고정 일정, 다른 시험의 공부 계획, 지금까지 실제로 완료한 분량을 GPT에 전달한다. GPT는 이 정보를 이용해 공부할 날짜, 시간, 범위를 제안한다.

다만 GPT의 결과를 그대로 무조건 저장하지는 않는다. 백엔드 코드가 다음 조건을 다시 검사한다.

- 시험 당일이나 시험 이후에 공부가 배정되지 않았는가?
- 공부 범위가 빠지거나 중복되지 않았는가?
- 목표 회독에 필요한 전체 분량과 GPT가 만든 전체 분량이 일치하는가?
- 종료 시간이 시작 시간보다 늦는가?
- 고정 일정이나 다른 시험 공부와 시간이 겹치지 않는가?

검사를 통과한 계획만 데이터베이스에 저장된다. 시간이 겹치면 코드는 15분 단위로 가까운 빈 시간을 찾아 옮긴다.

GPT가 반환한 재계획의 범위 시작점이 현재 진도와 정확히 이어지지 않거나 회독 경계에서 누락·중복이 생기면, 백엔드는 GPT가 선택한 날짜·시간과 상대적인 블록 크기는 유지하면서 범위 번호만 현재 완료 지점부터 연속되도록 자동 보정한다. 보정 후에도 전체 남은 분량이 목표량과 일치하는지 다시 검사한다.

## 2. 현재 제공하는 핵심 기능

### 시험 등록과 GPT 계획 생성

사용자는 다음 정보를 입력해 시험을 등록한다.

- 과목명
- 시험 날짜와 시험 시간
- 시험 범위의 시작과 끝
- 범위 단위(예: 페이지, 챕터)
- 목표 회독 수
- 우선 챕터 또는 교수님이 강조한 내용
- 과목 특성 및 계획 요구사항(자연어 자유 입력)

자유 입력란에는 `암기 과목이라 짧게 여러 번 보고 싶다`, `8월 29일에는 공부하지 않겠다`, `시험 전 이틀은 복습만 하겠다`처럼 일정 배분에 반영할 조건을 문장으로 적을 수 있다. 이 문장은 시험 정보와 함께 SQLite에 그대로 저장되며 최초 계획뿐 아니라 수행 기록 후 재계획을 만들 때도 OpenAI에 다시 전달된다. 다만 시험 전 계획 완료, 범위 누락 방지, 고정 일정 충돌 방지 같은 필수 규칙과 충돌하면 필수 규칙이 우선한다.

등록 버튼을 누르면 화면에는 실제 API 요청이 진행되는 동안에만 `계획 생성 중`이 표시된다. 백엔드는 고정 일정과 기존 공부 계획을 함께 확인한 후 OpenAI API를 호출한다. 현재 기본 모델은 `gpt-5-mini`이며 빠른 응답을 위해 `reasoning.effort`는 `minimal`로 설정되어 있다.

GPT는 다음 항목이 포함된 계획을 돌려준다.

- 계획에 대한 짧은 요약
- 공부 날짜
- 몇 번째 회독인지
- 그날 공부할 범위의 시작과 끝
- 추천 시작 시간과 종료 시간

저장된 체크인 기록이 있으면 백엔드는 원본 기록 전체 대신 개인 학습 패턴 요약을 추가로 전달한다. 여기에는 전체·동일 과목 표본 수, 완료율, 계획 대비 실제 수행 비율, 최근 5회 수행 비율, 평균 완료 분량, 성과가 좋았던 요일과 시간대가 포함된다. 동일 과목 기록이 2회 이상이면 해당 과목 패턴을 우선하고, 그렇지 않으면 전체 과목 기록을 참고한다. 기록이 적을 때는 GPT가 패턴을 확정적으로 가정하지 않도록 신뢰도를 낮춘다.

### 공부 결과 기록과 자동 재계획

각 공부 블록에는 세 가지 결과를 기록할 수 있다.

- `완료·초과`: 계획한 만큼 끝냈거나 계획보다 더 공부한 경우
- `일부`: 계획 범위 중 일부만 공부한 경우
- `미완료`: 공부하지 못한 경우

사용자가 실제 완료 지점을 입력하면 백엔드는 실제 공부량과 예정 공부량의 차이를 계산한다. 이후 아직 실행하지 않은 미래 계획을 제거하고, 완료된 전체 분량과 남은 기간을 기준으로 GPT에 새 계획을 요청한다.

재계획 후에는 다음 정보를 보여준다.

- 계획보다 얼마나 더 했거나 덜 했는지
- 계획 버전이 어떻게 바뀌었는지
- 현재 속도로 예상되는 회독 수
- 목표 회독을 유지해도 되는지 또는 공부 강도·목표 조정이 필요한지

과거에 완료한 기록은 삭제하지 않는다. 미래의 실행 전 계획만 새 계획으로 교체한다.

### 캘린더

캘린더에서는 다음 내용을 함께 볼 수 있다.

- 사용자가 등록한 수업·아르바이트·약속 등의 고정 일정
- GPT가 만든 과목별 공부 블록
- 시험 날짜와 시간
- 월간 캘린더와 선택한 날짜의 일간 타임테이블
- 주간 캘린더

월간 캘린더에서 날짜를 누르면 오른쪽 일간 타임테이블이 해당 날짜로 바뀐다. 월간과 일간 캘린더는 아래쪽 높이가 맞도록 구성되어 있다.

월간과 일간 사이의 손잡이를 좌우로 끌면 두 화면의 너비 비율을 조절할 수 있다. 이 비율은 데이터베이스가 아니라 현재 브라우저의 `localStorage`에 저장된다. 따라서 같은 브라우저에서는 새로고침해도 비율이 유지되지만, 다른 PC나 다른 브라우저에는 전달되지 않는다.

공부 계획과 시험은 과목명에 따라 색상이 정해진다. 같은 과목은 월간·주간·일간 화면에서 같은 색으로 표시된다.

### 일정 이동과 길이 변경

일간 타임테이블에서 다음 조작이 가능하다.

- 일정 블록의 위쪽 또는 본문을 끌어 시작·종료 시간을 함께 이동
- 일정 블록 아래쪽 손잡이를 끌어 종료 시간을 변경

시간은 15분 단위로 움직인다. 변경된 시간이 다른 고정 일정이나 공부 계획과 겹치면 백엔드가 저장을 거부하고 안내 메시지를 보여준다. 시험 시간 블록은 이동할 수 없다.

### 매주 반복 일정

같은 요일과 같은 시간에 반복되는 수업 등을 한 번에 등록할 수 있다.

사용자가 입력하는 정보는 다음과 같다.

- 일정명과 종류
- 반복 시작일
- 반복 종료일
- 시작 시간과 종료 시간
- `매주 반복` 선택 여부

매주 반복을 선택하면 시작일부터 종료일까지 7일 간격으로 각각의 일정이 생성되어 데이터베이스에 저장된다. 예를 들어 9월 1일부터 9월 22일까지 반복하면 9월 1일, 8일, 15일, 22일 일정이 생성된다. 이렇게 생성된 모든 일정은 GPT 계획 생성 시 공부할 수 없는 시간으로 사용된다.

### 대시보드

대시보드는 시험별로 다음 정보를 보여준다.

- 시험 날짜와 시간
- 목표 회독 수
- 현재 완료 회독 수
- 현재 학습 속도를 기준으로 한 예상 회독 수
- 남은 공부 블록 수
- 우선 챕터
- 과목 특성 및 계획 요구사항 원문
- 최근 재계획 이유와 조언

시험 삭제 버튼을 누르면 해당 시험과 연결된 공부 계획·공부 기록이 함께 삭제된다.

### 30초 데모

`30초 데모`는 기존 시험, 공부 계획, 공부 기록, 고정 일정을 모두 지우고 예시 수업·아르바이트·시험을 새로 만든다. 이 과정에서도 실제 OpenAI API를 호출해 예시 공부 계획을 생성한다.

따라서 이미 중요한 데이터를 입력한 상태에서는 데모 초기화를 누르지 않는 것이 좋다.

## 3. 지금 웹은 어디에서 실행되는가?

현재 버전은 인터넷에 배포된 하나의 공개 웹사이트가 아니라 사용자의 PC에서 실행되는 로컬 웹 서비스다.

```text
브라우저 화면
http://localhost:5173
        │
        │ HTTP 요청
        ▼
사용자 PC의 FastAPI 백엔드
http://127.0.0.1:8000
        │                 │
        │ 읽기·쓰기       │ 계획 생성 요청
        ▼                 ▼
backend/nexus.db       OpenAI API
SQLite 파일            인터넷상의 외부 서비스
```

두 프로그램의 역할은 다음과 같다.

- 프런트엔드: 사용자가 보는 화면이다. React, TypeScript, Vite로 구성되고 보통 5173번 포트에서 실행된다.
- 백엔드: 화면에서 보낸 요청을 처리하고 데이터베이스와 OpenAI API를 연결한다. Python과 FastAPI로 구성되고 8000번 포트에서 실행된다.

`localhost`와 `127.0.0.1`은 모두 현재 사용 중인 자기 PC를 뜻한다. 같은 ZIP 파일을 받은 다른 팀원이 실행하면 그 팀원의 PC 안에서 별도의 5173·8000번 포트를 사용한다. 다른 팀원의 실행이 내 PC의 포트를 차지하지 않는다.

GitHub 저장소는 소스 코드를 보관하고 팀원끼리 공유하는 장소다. GitHub에 코드를 올렸다고 해서 웹 서비스가 자동으로 실행되거나 사용자 기록이 GitHub로 전송되는 것은 아니다.

## 4. 기록은 어디에 저장되는가?

### 주요 기록: SQLite 데이터베이스

기본 설정에서는 다음 파일에 기록이 저장된다.

```text
C:\DataFolder\2026\넥서스챌린지\backend\nexus.db
```

백엔드를 `backend` 폴더에서 실행하면 `.env`의 기본 설정인 `sqlite:///./nexus.db`도 위 파일을 가리킨다.

SQLite는 별도의 데이터베이스 프로그램을 설치하지 않고 파일 하나에 표 형태로 정보를 저장하는 방식이다. 백엔드를 종료하거나 브라우저를 닫아도 `nexus.db` 파일이 남아 있는 동안 기록은 유지된다.

`.gitignore`에는 `*.db`가 포함되어 있으므로 이 데이터베이스 파일은 일반적인 Git 커밋과 푸시에 포함되지 않는다. 즉, 사용자의 시험·공부 기록은 GitHub에 자동 업로드되지 않는다.

### 데이터베이스에 저장되는 표

#### `calendar_events` — 고정 일정

- 일정명
- 일정 종류: 수업, 아르바이트, 약속, 기타
- 시작 날짜와 시간
- 종료 날짜와 시간

반복 일정도 실제로는 이 표에 날짜별 개별 일정으로 저장된다.

#### `exams` — 시험

- 과목명
- 시험 날짜와 시간
- 시험 범위와 단위
- 목표 회독 수
- 현재 계획 버전
- GPT가 만든 계획 요약
- 우선 챕터
- 최근 재계획 설명
- 현재 속도에 대한 조언

#### `study_tasks` — 공부 계획 블록

- 어떤 시험에 속하는 계획인지
- 공부 날짜
- 회독 번호
- 공부할 범위
- 예정 분량
- 상태: 계획됨, 완료, 일부 완료, 미완료
- 계획 버전
- 추천 시작·종료 시간

#### `study_logs` — 실제 수행 기록

- 어떤 공부 계획에 대한 기록인지
- 완료, 일부 완료, 미완료 중 어떤 결과인지
- 실제 완료 분량
- 실제 완료 지점
- 기록한 시각

### 브라우저에만 저장되는 정보

월간·일간 캘린더 사이의 너비 비율만 브라우저 `localStorage`에 저장된다. 시험이나 공부 기록은 브라우저 저장소에 보관하지 않는다.

### 환경 설정과 API 키

OpenAI API 키와 모델명은 `backend/.env`에 저장한다.

```env
OPENAI_API_KEY=실제_API_키
OPENAI_MODEL=gpt-5-mini
TIMEZONE=Asia/Seoul
```

`.env`는 Git에서 제외된다. API 키는 프런트엔드로 전달하거나 SQLite에 저장하지 않고, 백엔드가 OpenAI에 요청할 때만 사용한다. API 키를 화면 코드나 `VITE_`로 시작하는 변수에 넣으면 브라우저 사용자에게 노출될 수 있으므로 그렇게 하면 안 된다.

## 5. 어떤 정보가 어디로 이동하는가?

### 화면에서 백엔드로 전달되는 정보

브라우저는 사용자의 입력을 JSON이라는 정해진 문서 형태로 백엔드에 보낸다.

| 사용자의 행동 | 백엔드로 보내는 정보 | 사용 목적 |
|---|---|---|
| 시험 등록 | 과목, 시험 날짜·시간, 범위, 단위, 목표 회독, 우선 챕터, 과목 특성·계획 요구사항 | 시험 저장 및 최초 계획 생성 |
| 일정 등록 | 일정명, 종류, 시작·종료 시각 | 공부 불가능 시간 저장 |
| 반복 일정 등록 | 일정명, 종류, 시작일·종료일, 시작·종료 시간 | 주 단위 고정 일정 생성 |
| 공부 결과 기록 | 완료 상태, 실제 완료 지점 | 실제 분량 계산 및 재계획 |
| 블록 이동·크기 변경 | 변경된 시작·종료 시간 | 충돌 검사 후 일정 수정 |
| 시험 삭제 | 시험 번호 | 시험과 연결 기록 삭제 |

프런트엔드는 기본적으로 `http://localhost:8000`의 API를 호출한다.

### 백엔드에서 OpenAI로 전달되는 정보

GPT 계획이 필요한 경우 백엔드는 다음 정보를 OpenAI에 전달한다.

- 계획을 시작할 날짜
- 과목명
- 시험 날짜
- 시험 범위와 단위
- 목표 회독 수
- 우선 챕터
- 사용자가 자연어로 작성한 과목 특성 및 계획 요구사항 원문
- 지금까지 완료한 전체 분량
- 앞으로 남은 전체 분량
- 저장된 기록에서 계산한 완료율·수행 비율·최근 추세·강한 요일과 시간대
- 고정 일정의 제목과 시작·종료 시각
- 다른 시험 공부 계획의 날짜와 시작·종료 시각

OpenAI에는 SQLite 파일 전체나 공부 기록 한 건 한 건을 그대로 보내지 않는다. 백엔드가 완료 분량과 학습 패턴 통계를 계산한 뒤 요약된 숫자와 요일·시간대 목록만 보낸다.

고정 일정의 제목은 OpenAI 요청에 포함되므로 일정명에 민감한 개인정보를 입력했다면 해당 문자열도 외부 API로 전달될 수 있다. 민감한 일정은 `개인 일정`처럼 일반적인 이름으로 입력하는 것이 안전하다.

### OpenAI에서 백엔드로 돌아오는 정보

OpenAI는 정해진 JSON 형식으로 다음을 반환한다.

- 전체 계획 요약
- 날짜별 공부 계획 목록
- 각 계획의 회독 번호와 범위
- 추천 시작·종료 시간

백엔드는 이 응답을 검증하고 충돌을 조정한 뒤 SQLite에 저장한다. OpenAI 호출이 실패하거나 응답이 규칙에 맞지 않으면 시험 등록 또는 재계획 요청은 실패 처리되며, 임의의 코드 기반 계획으로 몰래 대체하지 않는다.

### 백엔드에서 화면으로 돌아오는 정보

화면이 전체 현황을 요청하면 백엔드는 고정 일정, 시험, 공부 계획, 현재 회독 수, 예상 회독 수, 재계획 설명을 한꺼번에 반환한다. 프런트엔드는 이 결과를 대시보드와 캘린더에 표시한다.

## 6. GPT는 무엇을 기준으로 어떻게 계획을 만드는가?

현재 방식은 사용자가 ChatGPT 입력창에 프롬프트를 쓰는 것과 기본 원리가 같다. 다만 백엔드가 사람이 쓴 한 문장 대신 요청을 **고정 지시문**과 **이번 계획의 실제 데이터**로 나누어 OpenAI Responses API에 보낸다. 이전 대화를 기억하는 챗봇 방식은 아니며, 최초 계획과 재계획을 요청할 때마다 그 시점에 필요한 정보를 새로 정리해 전달한다.

### GPT에게 보내는 실제 데이터

백엔드는 다음 내용을 JSON이라는 정해진 데이터 형식으로 묶는다.

- 오늘 날짜와 계획을 시작할 날짜
- 과목명, 시험 날짜, 시험 범위, 범위 단위, 목표 회독 수
- 우선 챕터와 사용자가 자유 입력란에 쓴 과목 특성·계획 요구사항 원문
- 지금까지 완료한 누적 분량과 앞으로 남은 분량
- 다음에 시작해야 하는 정확한 회독 번호와 범위 시작점
- 수업·아르바이트·약속 같은 고정 일정
- 다른 시험에 이미 배정된 공부 블록
- 저장된 수행 기록에서 계산한 완료율, 계획 대비 실제 수행 비율, 최근 추세, 성과가 좋았던 요일과 시간대, 패턴 신뢰도

예를 들어 사용자가 `암기 과목이라 짧게 여러 번 보고 싶다. 8월 29일에는 공부하지 않고, 시험 전 이틀은 복습만 하겠다.`라고 입력하면 이 문장은 별도로 요약하거나 바꾸지 않고 `planning_preferences`라는 항목에 그대로 넣는다. SQLite에도 같은 원문을 저장하므로 최초 계획뿐 아니라 이후 재계획에도 다시 전달된다.

공부 기록은 한 건씩 통째로 보내지 않는다. 백엔드가 먼저 통계를 계산하고, GPT에는 계획에 필요한 요약값만 보낸다. 따라서 GPT는 사용자의 과거 대화를 기억해서 개인화하는 것이 아니라, 백엔드가 매 요청에 포함한 저장 기록 요약을 기준으로 개인화한다.

### GPT에게 내리는 고정 주문

백엔드는 GPT를 `대학생 시험 계획을 만드는 일정 최적화 엔진`으로 지정하고 다음 규칙을 지시한다.

- 시험 당일에는 공부를 배정하지 않는다.
- 고정 일정, 다른 시험의 공부 계획과 시간이 겹치지 않게 한다.
- 우선 챕터는 앞쪽 날짜와 집중하기 좋은 시간에 우선 배치한다.
- 자연어 요구사항을 날짜별 분량, 반복 빈도, 공부 시간과 복습 배치에 반영한다.
- 범위를 회독 순서대로 빠짐없이 배정하고 같은 회독에서 중복하지 않는다.
- 일정이 많은 날에는 학습량을 줄이고 가능한 시간대를 선택한다.
- 학습 패턴의 신뢰도가 충분하면 실제 수행률과 성과가 좋았던 요일·시간대를 적극 반영한다.
- 평균 수행 비율이 낮으면 한 블록의 분량을 작게 나누고, 높으면 감당 가능한 범위에서 늘린다.
- 개인화하더라도 목표 총량을 줄이거나 시험 범위를 생략하지 않는다.
- 결과는 백엔드가 읽을 수 있는 정해진 JSON 형식으로 반환한다.

자유 입력 요구사항은 중요한 개인화 조건이지만 최상위 규칙은 아니다. 예를 들어 사용자가 요청한 하루 분량만으로 시험 전 목표를 끝낼 수 없다면, 시험 기간·목표량·범위 연속성·일정 충돌 방지 같은 필수 조건을 우선하도록 지시한다.

사람이 읽기 쉽게 단순화하면 GPT에 보내는 요청은 다음과 같은 모양이다.

```text
[고정 지시문]
당신은 대학생 시험 계획을 만드는 일정 최적화 엔진이다.
시험 당일 배정 금지, 일정 충돌 금지, 범위 누락·중복 금지 등의 규칙을 지켜라.
사용자의 요구사항과 저장된 학습 패턴을 반영하라.

[이번 요청의 데이터]
과목: 생화학
시험일: 9월 10일
범위와 목표: 1~200페이지, 2회독
현재 진도: 1회독 50페이지 완료, 다음 시작점 51페이지
요구사항: 암기 과목이라 짧게 여러 번 보고 싶다
사용 불가능 시간: 월요일 13:00~15:00 수업
학습 패턴: 저녁 시간대 실제 수행 비율이 높음
```

실제 전송은 위와 같은 자연어 문서가 아니라 구조화된 JSON 데이터와 고정 지시문으로 이루어진다.

### GPT가 결정하는 것과 백엔드가 결정하는 것

GPT는 받은 정보를 바탕으로 공부 날짜, 날짜별 분량, 회독과 범위, 추천 시작·종료 시간, 반복 빈도, 복습 배치와 계획 요약을 제안한다. 즉 남은 분량을 날짜 수로 균등하게 나누는 코드가 날짜와 시간을 정하는 것이 아니라 GPT가 일정 맥락과 개인화 정보를 함께 보고 제안한다.

하지만 GPT 결과를 그대로 저장하지는 않는다. 백엔드는 시험 기간 밖의 날짜, 시험 범위 이탈, 회독 순서의 누락·중복, 목표 총량 불일치, 잘못된 시간 형식과 일정 충돌을 다시 검사한다. 범위 계산이 어긋났다면 GPT가 고른 날짜·시간과 블록 크기 비율은 최대한 유지하면서 현재 진도부터 범위 번호가 연속되도록 보정한다. 시간이 겹치면 가까운 빈 시간대로 옮긴다. 이 검증을 통과한 결과만 SQLite와 캘린더에 반영한다.

따라서 역할은 다음처럼 나뉜다.

```text
사용자 입력·저장 기록 → 백엔드가 프롬프트 데이터로 정리
→ GPT가 날짜·시간·분량 계획 제안
→ 백엔드가 계산과 충돌을 검증·보정
→ SQLite 저장 → 캘린더 표시
```

## 7. 계획 생성의 전체 흐름

```text
1. 사용자가 시험 정보를 입력한다.
2. 프런트엔드가 입력값을 FastAPI 백엔드에 보낸다.
3. 백엔드가 시험일이 오늘 이후인지, 범위가 올바른지 검사한다.
4. SQLite에서 고정 일정과 다른 시험의 공부 계획을 읽는다.
5. 완료 분량과 목표 회독 기준의 남은 분량을 계산한다.
6. 필요한 정보만 정리해 OpenAI에 보낸다.
7. GPT가 날짜·시간·범위별 계획을 반환한다.
8. 백엔드가 누락, 중복, 날짜, 분량, 시간 형식을 검증한다.
9. 일정이 겹치면 15분 단위로 가까운 빈 시간에 배치한다.
10. 시험과 공부 계획을 SQLite에 저장한다.
11. 완성된 데이터를 브라우저에 돌려주고 캘린더에 표시한다.
```

시험 등록 과정 중 하나라도 실패하면 데이터베이스 작업을 취소한다. 따라서 GPT 호출이 실패했는데 시험만 불완전하게 저장되는 상황을 줄이도록 설계되어 있다.

## 8. 재계획과 예상 회독 계산 방식

사용자가 공부 결과를 기록하면 실제 완료 분량을 `study_logs`에 저장한다. 이후 해당 시험에 남아 있는 실행 전 계획을 삭제하고, 다음 날부터 시험 전날까지의 계획을 GPT에 다시 요청한다.

예상 회독은 다음 방식으로 계산한다.

- 아직 수행 기록이 없으면: 현재 계획에 들어 있는 전체 예정 분량을 기준으로 계산
- 수행 기록이 있으면: 지금까지 실제 완료한 분량 ÷ 기록이 있는 날짜 수로 하루 평균 속도를 구한 뒤, 시험 전까지 같은 속도가 이어진다고 가정

예상 회독이 목표보다 낮으면 공부 강도를 높이거나 목표 회독을 조정하라는 안내를 표시한다. 이는 확정된 미래 결과가 아니라 현재 기록을 바탕으로 한 단순 예측이다.

## 9. 시간 충돌은 어떻게 막는가?

GPT에게도 일정을 겹치지 않게 만들도록 요청하지만, 백엔드가 한 번 더 검사한다.

- 고정 일정
- 다른 시험의 공부 계획
- 같은 시험에서 이미 배치한 공부 계획

위 시간과 겹치면 GPT가 추천한 시간부터 15분씩 뒤로 확인한다. 자리가 없으면 오전 6시부터 다시 확인한다. 공부 블록 길이는 최소 30분, 최대 4시간 범위로 조정된다. 하루 안에서 빈 시간을 찾지 못하면 오류를 보여주고 저장하지 않는다.

서버를 시작할 때는 이전 버전에서 만들어진 겹치는 공부 블록도 찾아 가까운 빈 시간으로 옮기는 보정 작업을 실행한다.

## 10. 사용되는 API 주소

| 주소 | 역할 |
|---|---|
| `GET /health` | 백엔드 상태, 시간대, OpenAI 설정 여부 확인 |
| `GET /api/overview` | 일정·시험·공부 계획 전체 조회 |
| `POST /api/events` | 한 번만 있는 고정 일정 등록 |
| `POST /api/events/recurring` | 매주 반복 일정 등록 |
| `PATCH /api/events/{id}/time` | 고정 일정 시간 변경 |
| `POST /api/exams` | 시험 등록 및 GPT 최초 계획 생성 |
| `DELETE /api/exams/{id}` | 시험과 연결 데이터 삭제 |
| `PATCH /api/tasks/{id}/time` | 공부 블록 시간 변경 및 충돌 검사 |
| `POST /api/tasks/{id}/check-in` | 실제 공부 결과 기록 및 GPT 재계획 |
| `POST /api/demo/reset` | 전체 데이터 초기화 후 데모 데이터 생성 |

API를 직접 확인하려면 백엔드를 실행한 뒤 `http://127.0.0.1:8000/docs`를 열면 된다.

## 11. 로컬 실행 방법

백엔드와 프런트엔드는 서로 다른 터미널에서 동시에 실행해야 한다.

### 터미널 1: 백엔드

```powershell
cd "C:\DataFolder\2026\넥서스챌린지\backend"
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

### 터미널 2: 프런트엔드

```powershell
cd "C:\DataFolder\2026\넥서스챌린지\frontend"
& "C:\Program Files\nodejs\npm.cmd" run dev
```

브라우저에서 터미널에 표시된 주소를 연다. 일반적으로 `http://localhost:5173`이다. 5173번 포트를 이미 다른 프로그램이 사용 중이면 Vite가 5174처럼 다른 포트를 안내할 수 있다.

백엔드의 8000번 포트가 이미 사용 중이라는 오류가 나오면 기존 백엔드가 실행 중인지 먼저 확인해야 한다. 같은 PC에서 같은 포트는 한 프로그램만 사용할 수 있다.

## 12. 현재 버전에서 알아둘 제한사항

- 회원가입과 로그인이 없다. 같은 `nexus.db`를 사용하는 사람은 같은 데이터를 보게 된다.
- 데이터는 현재 PC의 SQLite 파일에만 있으므로 다른 PC와 자동 동기화되지 않는다.
- 데이터베이스 파일을 삭제하면 시험과 학습 기록도 사라진다. 중요한 기록은 파일을 별도로 백업해야 한다.
- 반복 일정은 하나의 반복 규칙으로 관리되는 것이 아니라 날짜별 개별 일정으로 저장된다. 현재는 반복 묶음 전체 삭제 기능이 없다.
- 일정 블록 이동은 같은 날짜 안에서 시간만 변경한다. 다른 날짜로 이동하는 기능은 없다.
- OpenAI API 키, 인터넷 연결, API 사용 한도 또는 결제 상태에 문제가 있으면 계획 생성과 재계획이 실패한다.
- GPT가 계획을 만들지만 백엔드의 엄격한 검사를 통과하지 못하면 저장되지 않는다.
- GPT 응답의 회독·범위 연속성 오류는 자동 보정을 먼저 시도하며, 날짜·시간이나 전체 목표량까지 안전하게 복구할 수 없는 경우에만 재계획을 중단한다.
- 예상 회독은 실제 하루 평균이 앞으로도 유지된다는 가정에 기반한 참고값이다.
- 현재는 실제 타이머로 공부 시간을 측정하지 않는다. 시간대 패턴은 계획 블록의 시간과 그 블록의 체크인 결과를 연결해 추정한다.
- 현재 CORS 설정은 프런트엔드가 5173번 포트에서 실행되는 경우를 허용한다. Vite가 5174번 포트로 실행되면 브라우저 요청이 차단될 수 있으므로 가능하면 5173번 포트를 사용해야 한다.

## 13. 한 문장으로 정리

RE:PLAN은 **사용자의 시험·일정·실제 공부 기록을 로컬 SQLite에 보관하고, 계획에 필요한 요약 정보를 OpenAI GPT에 보내 공부 계획을 생성한 뒤, 백엔드가 계획의 범위와 시간 충돌을 검증하여 캘린더에 표시하는 로컬 학습 계획 서비스**다.
